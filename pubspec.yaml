name: cash_helper_app
description: A new Flutter project.

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.18.6 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

  cupertino_icons: ^1.0.2
  firebase_core: ^2.4.0
  cloud_firestore: ^4.3.0
  firebase_auth: ^4.2.4
  flutter_modular: ^5.0.0
  uuid: ^3.0.7
  dio: ^5.1.1
  bcrypt: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter

  mockito: ^5.3.2
  fake_cloud_firestore: ^2.3.0+3
  firebase_auth_mocks: ^0.11.0
  flutter_lints: ^2.0.0
  mocktail: ^0.3.0

flutter:
  uses-material-design: true
