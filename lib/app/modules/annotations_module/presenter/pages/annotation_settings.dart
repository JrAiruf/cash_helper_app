import 'package:flutter/material.dart';

class AnnotationSettings extends StatelessWidget {
  const AnnotationSettings({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}